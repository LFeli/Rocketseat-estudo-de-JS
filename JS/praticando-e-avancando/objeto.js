// Object

const person = {
    name: 'Jonh',
    age: 30 ,
    weight: 88.6,
    isAdmin: true
}

// select something specific in the object
console.log(person.name);

// interpolação
console.log(`O ${person.name} tem ${person.age} anos`);